services:
  app:
    build: .
    depends_on:
      - db

  db:
    image: mysql:latest
    container_name: db
    environment:
      MYSQL_ROOT_PASSWORD: "example"
      MYSQL_DATABASE: "employees"
    ports:
      - "3306:3306"